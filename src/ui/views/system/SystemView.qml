import QtQuick 2.7
import QtQuick.Controls 2.5
import QtQuick.Layouts 1.3
import org.kde.kirigami 2.7 as Kirigami
import org.mauikit.controls 1.0 as Maui

//for now znx components are within nx store project.
//those should be moved as a separate component avalible for the wide system ? or into a separete repo?
//the idea is to reuse it on different places: single app and nx store
import "../../znx"

DevicesPage
{
    id: control
}
