dist: focal

script:
  - sudo ./travis-build.sh

branches:
  only:
    - dev-mauikit-ui

deploy:
  - provider: script
    script: wget -qO - https://raw.githubusercontent.com/Nitrux/nitrux-repository-util/master/upload-pkgs | sh -s *.deb
    skip_cleanup: true
    on:
      branch: dev-mauikit-ui

notifications:
  email: false
  